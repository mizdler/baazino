   <div class="container">
		<div class="title">
			<h2>بیشترین دانلود</h2>
			<div class="more"><a href="/dowloadedgames">بیشتر</a></div>
		</div>
    <% all_downloads.each do |game| %>

		<article class="app">
			<figure><a href=""><img src="tank.webp" alt="" class="thumb"></a></figure>
			<h2><%= link_to game.game_name, game %></h2>
			<div class="cat"><a href=""><%= game.description %></div>
			<div class="price"><% if game.price!=0 %><%= game.price %> تومان<%else%>رایگان<%end%> </div>
		</article>
    <% end %>
    </div>